## TODOs
- Clean up tests (don't think I'm doing them correctly...)
    - Write test name - get assert setup
    - Write function for said test
    - Call function from test to assert

- Move "completed" code outside of "main" file or where it makes sense.

## Roadmap
- [ ] Command Line Argument parsing of image path
    - May need to move around/add flags but should technically be done
- [ ] Parsing of image pixels
    - [ ] Get structs set up
    - [ ] Get Conversion functions setup
    - [ ] 
